package Sprint4_quizkamp_server.Server;

import java.io.IOException;
import java.net.InetAddress;
import java.net.ServerSocket;

public class TcpListener 
{
    public static void main(String[] args) throws IOException 
    { 
        /*
        final int port = 12345;
        ServerSocket serverSocket = new ServerSocket(port);
        System.out.print("Listening - " + InetAddress.getLocalHost() + ":" + port);
        
        TcpServer player1, player2;
        
        while (true) 
        {
            try 
            {
                player1 = new TcpServer(serverSocket.accept());
                System.out.println("P1 connected!");

                player2 = new TcpServer(serverSocket.accept());
                System.out.println("P2 connected!");
                
                player1.setOpponent(player2);
                player2.setOpponent(player1);
            }
            
            catch (IOException e)
            {
                e.printStackTrace();
            }
        } 
*/
    }
}
